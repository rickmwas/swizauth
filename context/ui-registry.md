# UI Component Registry: SWIZAUTH

This registry tracks the reusable React/Next.js UI components built for the SwizAuth system.

*No components have been registered yet. Components will be logged here as they are developed.*
